import { ModalsDirective } from './modals.directive';

describe('ModalsDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalsDirective();
    expect(directive).toBeTruthy();
  });
});
